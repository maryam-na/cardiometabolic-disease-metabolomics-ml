# Notebooks

Use this folder for exploratory analyses that complement the scripted pipeline. Keep production preprocessing, modeling, and evaluation logic in `src/` so the Streamlit app and training workflow stay reproducible.
